import React from 'react';

function Header() {
    return (
        <header>
            <h1>Magical Employee Directory</h1>
            <p>
                Use the search bar or order employees by using the sort arrow
            </p>
        </header>
    );
}

export default Header;